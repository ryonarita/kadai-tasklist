<!-- <div class="row">  
<div class="col-xs-12 col-sm-6 col-sm-offset-2 col-md-6 col-md-offset-2 col-lg-3 col-lg-offset-3"> -->
<%#= form_for(task, html:{class: "form-horizontal"}) do |f| %>
<%= form_for(task) do |f| %>
  <% if task.errors.any? %>
    <!--
    <div id="error_explanation">
    -->
    <div class="alert alert-warning">
      <!-- 
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>
      -->
      <ul>
        <% task.errors.full_messages.each do |task| %>
          <li><%= task %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <!--
  <div class="form-group">
    <%= f.label :status, '状態(最大10文字)', class:"col-xs-3" %>
    <div class="col-xs-6"><%= f.text_field :status, class: 'form-control' %></div>
  </div>
  <div class="form-group">
    <%= f.label :content, 'タスク(最大255文字)', class:"col-xs-3" %>
    <div class="col-xs-6"><%= f.text_field :content, class: 'form-control' %>
  </div>
  -->
  
  
  <div class="form-group">
    <%= f.label :status, '状態'%>
    <%= f.text_field :status, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :content, 'タスク' %>
    <%= f.text_field :content, class: 'form-control' %>
  </div>
  
  <!-- HTMLだけで書く場合
  <div class="form-group">
    <label for="task">Content of Task </label>
    <input type="text" class="form-control" id="task" placeholder="Task">
  </div>
  
  <div class="form-group">
    <label for="status">Status of Task </label>
    <input type="text" class="form-control" id="status" placeholder="Status">
  </div>
  <button type="submit" class="btn btn-default">Submit</button>
  -->
  
  <div class="form-group">
  <%= f.submit '投稿', class: 'btn btn-primary' %>
  </div>
<% end %>
</div>
</div>