<h1>id = <%= @task.id %> のタスクの詳細情報</h1>


<!--
<p>状態: <%= @task.status %></p>
<p><%= @task.content %></p>
 -->
<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @task.id %></td>
  </tr>
  <tr>
    <th>状態</th>
    <td><%= @task.status %></td>
  </tr>
  <tr>
    <th>タスク</th>
    <td><%= @task.content %></td>
  </tr>
</table>

<!-- 
<%= link_to '一覧に戻る', tasks_path, class: 'btn btn-default' %>
 -->
<%= link_to 'このタスクを編集する', edit_task_path(@task), class: 'btn btn-default' %>
<%= link_to 'このタスクを削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>